import React from 'react';

const Settings: React.FC = () => {
  return (
    <section>
      <h2>Settings</h2>
      <p>Configure PIN, sounds, and sync.</p>
    </section>
  );
};

export default Settings;
